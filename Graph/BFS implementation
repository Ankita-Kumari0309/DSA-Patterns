import java.util.*;
class Main
{
	public static void main(String args[])
	{
		Scanner sc = new Scanner(System.in);
		System.out.println("Enter the no of vertices");
		int V = sc.nextInt();
		System.out.println("Enter the no of edges");
		int E = sc.nextInt();
		
		ArrayList<ArrayList<Integer>> adj = new ArrayList<>();
		
		for(int i = 0;i<V;i++)
		{
			adj.add(new ArrayList<>());
		}
		
		for(int i =0;i<E;i++)
		{
			int u = sc.nextInt();
			int v = sc.nextInt(); 
			adj.get(u).add(v);
		    adj.get(v).add(u);	
		}
		System.out.println("The graph is:");
		for(int i = 0;i<V;i++)
		{
			System.out.println(i + "->" + adj.get(i));
			
		}
		System.out.println("The BFS graph traversal is:");
		boolean[] visited = new boolean[V]; 
		for(int i = 0;i<V;i++)
		{
			 if(!visited[i])
			 bfs(adj,i,visited);
		}
	}
	public static void bfs(ArrayList<ArrayList<Integer>> adj,int Source,boolean[] visited)
	{
	    Queue<Integer> q = new LinkedList<>();
	    q.add(Source);
	    visited[Source] = true;
	    
	    while(!q.isEmpty())
	    {
	        int u = q.poll();
	        System.out.println(u+ " ");
	        for(int n : adj.get(u))
	        {
	            if(!visited[n])
	            {
	               visited[n] = true;
	               q.offer(n);
	            }
	        }
	    }
	    
	}
}
		

## Output
Enter the no of vertices
5
Enter the no of edges
4
0 1
0 2
1 3
2 4
The graph is:
0->[1, 2]
1->[0, 3]
2->[0, 4]
3->[1]
4->[2]
The BFS graph traversal is:
0 
1 
2 
3 
4 



## Time Complexity O(V + E)
## Auxiliary Space: O(V) (recursion stack + visited array)
## Total Space (including graph): O(V + E)
